# Example environment variables
# Copy to .env and adjust as needed

# Service URLs
MEMORY_URL=http://localhost:4001/memory/v1
PROCESSOR_URL=http://localhost:4002/processor/v1
PROMPT_URL=http://localhost:4003/prompt/v1
ACTION_URL=http://localhost:4004/action/v1

# Database (reserved for future wiring)
PGHOST=localhost
PGPORT=5432
PGUSER=ellipsa
PGPASSWORD=ellipsa
PGDATABASE=ellipsa
